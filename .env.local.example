# Copia questo file in .env.local e compila con le chiavi del tuo progetto Supabase
# (Project Settings -> API, nel dashboard Supabase).

NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Solo server-side: mai esporre al client, mai committare un valore reale.
SUPABASE_SERVICE_ROLE_KEY=

# Web Push (VAPID) - generare con: npx web-push generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=

# URL pubblico del sito (usato nei link delle email, es. reset password)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Protegge /api/cron/* (Vercel imposta automaticamente questo header sulle
# chiamate cron quando la variabile e' configurata sul progetto Vercel).
CRON_SECRET=

# Email (promemoria pagamenti) - registrarsi gratis su resend.com per la chiave.
# Senza questa chiave l'invio email e' semplicemente disattivato (no-op).
RESEND_API_KEY=
RESEND_FROM_EMAIL=Open Dance <onboarding@resend.dev>

# Bypass TEMPORANEO del login per vedere le aree protette senza un progetto
# Supabase reale. Valori: admin | staff | insegnante | genitore | allievo_adulto.
# Attivo solo in sviluppo (mai in produzione). Lasciare vuoto/rimuovere una
# volta collegato un progetto Supabase vero.
# NEXT_PUBLIC_DEV_BYPASS_ROLE=admin
